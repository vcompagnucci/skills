# Glossary

Every term the 125 sources coin or use in their own sense, in alphabetical order. OpenAI product names that aren't concepts are left out.

- **Ad-hoc notes.** User-requested memory edits dropped as small files for consolidation to apply. (`repo-memory`)
- **Agent legibility.** Optimizing the repo so an agent can reason about the whole domain from it. (`harness-eng`)
- **Agent loop / harness.** The core logic orchestrating user, model, and tool calls. (`agent-loop`)
- **Agentic loop.** A structured loop for reasoning and acting, where the model investigates, consults tools, and reports back, keeping private notes between steps. (`responses-api`)
- **Agent-native APIs.** Interfaces built around multi-step tool use and reasoning rather than single chat turns. (`devs-2025`)
- **Agent-native telemetry.** Logs that capture prompts, approvals, and tool results so defenders can see intent. (`codex-safely`)
- **Agents as tools.** A manager calls specialist agents like tools and keeps control of the conversation. (`sdk-multi-agent`)
- **Ambition vs. precision.** The rule that creativity scales with how little prior context constrains the task. (`repo-system-prompts`)
- **Approval interruption.** A paused run holding a pending tool call until someone approves or rejects it. (`docs-guardrails-review`)
- **Auto-review mode.** A subagent that auto-approves low-risk boundary-crossing requests so the user isn't interrupted. (`codex-safely`)
- **Backend executor behind an intermediary.** How the working agent is framed during voice: it never talks to the user directly. (`repo-realtime`)
- **Blended cost per verified resolution.** All model, tool, infra, retry, human-review, escalation and rework costs divided by verified issues resolved. (`cost-quality`)
- **Blocked stop condition.** When to stop and report that no defensible path remains, and what would unlock progress. (`codex-goals`)
- **Blocking vs parallel execution.** Whether a guardrail finishes before the agent starts or runs alongside it. (`sdk-guardrails`)
- **Build log.** The append-only record of observed progress and verification evidence, separate from the plan. (`codex-iterating`)
- **Codex enrichment.** Deriving a table's meaning from the code that produces it. (`data-agent`)
- **Collaboration mode.** A harness-set operating mode (Default or Plan) that changes what the agent may do and how much it asks. (`repo-multi-agent`)
- **Compaction item.** A model-produced, token-efficient representation of prior conversation state carried into the next window. (`computer-env`)
- **Compaction.** Replacing the conversation input with a smaller representative set of items once a token threshold is exceeded. (`agent-loop`)
- **Compatibility gates.** Checks run before a migration ships: endpoint support, cache topology, context and cost, parsers, state replay, mixed-model routing. (`repo-prompting`)
- **Completion audit / blocked audit.** The requirement-by-requirement checks the agent must pass before it may mark a goal complete or blocked. (`repo-goals`)
- **Context asymmetry.** Each party has partial knowledge and none sees the whole system. (`chatgpt-apps-lessons`)
- **Context checkpoint compaction.** Summarizing the thread into a handoff for a fresh model instance. (`repo-context`)
- **Context package.** The structured output of the context-builder stage, input to the reasoning stage. (`responses-year`)
- **Contextual evals.** Evals built for a specific product or workflow, as opposed to general frontier evals. (`business-evals`)
- **Contextual user fragment.** The shared trait every injected context item implements (role, kind, markers, body). (`repo-context`)
- **Control evaluations.** Estimating monitor recall on adversarially built distributions, including whether the monitor could collude with the agent. (`agent-monitoring`)
- **Conversational surface.** The voice model's role: talk to the user while the backend does the work. (`repo-realtime`)
- **Coverage / Restraint / Retention / Actionability.** The four evaluation questions for rule-guided review. (`review-rules`)
- **Credential probing.** Pulling credentials from unintended sources (browser profiles, logs) after normal auth failed. (`repo-approvals`)
- **Datapoint analysis pipeline.** The automated filter that reviews task metadata, model attempts, and failure traces to flag likely eval flaws. (`coding-evals-noise`)
- **Decentralized pattern / handoff.** A one-way transfer of execution and conversation state from one agent to a peer. (`practical-guide`, `sdk-multi-agent`)
- **Decision complete.** A plan detailed enough that the implementer does not need to make any decisions. (`repo-multi-agent`)
- **Deferred discovery.** Making tools, skills, and plugins surfaceable only when needed instead of loading all of them into context. (`gpt56-efficiency`, `repo-tools`)
- **Delegate / Review / Own.** The three-way split of responsibility between agent and humans for each phase of the lifecycle. (`ai-native-team`)
- **Delegation.** The voice model handing reasoning or tool use to a frontier model off the live path. (`gpt-live`)
- **Discoverable facts vs preferences/tradeoffs.** The two kinds of unknowns: explore for the first, ask for the second. (`repo-multi-agent`)
- **Doc-gardening agent.** Recurring agent that finds stale docs and opens fixes. (`harness-eng`)
- **Done when.** The prompt element stating what must be true before the task is complete. (`codex-best-practices`)
- **Durable project memory.** Spec, plan, constraints and status kept in files the agent re-reads to prevent drift and hold a stable definition of done. (`long-horizon`)
- **Durable thread.** A pinned, long-lived thread that is the home for a workstream. (`codex-maxxing`)
- **Evaluation flywheel.** Run, document the workflow, improve agent workflows, run the next one faster. (`repetitive-work`)
- **ExecPlan.** A self-contained, living execution plan a coding agent follows from design to implementation. (`exec-plans`)
- **Failure shield.** A memory of symptom, cause, fix, and verification that prevents repeating a failure. (`repo-memory`)
- **First-pass implementer.** The agent's role: produce the initial version that humans shape and approve. (`ai-native-team`)
- **Full-duplex.** A voice model that listens and speaks at the same time. (`gpt-live`)
- **Goal.** A thread-scoped completion contract combining durable objective state, lifecycle controls, continuation policy, budget accounting, and evidence-based completion. (`codex-goals`)
- **Gold patch.** The original human-written fix used as the ground-truth reference. (`swe-bench-verified`)
- **Golden principles.** Opinionated mechanical rules enforced continuously by cleanup agents. (`harness-eng`)
- **Golden set.** A living reference of example inputs mapped to the outputs experts consider great. (`business-evals`)
- **Guardian.** The separate reviewer model that approves or denies planned agent actions. (`repo-approvals`)
- **Guardrail feedback loop.** Automated eval, threshold adjustment, and re-validation until precision and recall targets are met. (`governance`)
- **HALO (Hierarchical Agent Loop Optimization).** Method that analyzes traces for recurring harness-level failures and writes a ranked report for a coding agent. (`improvement-loop`)
- **Harness engineering.** Designing environments, feedback loops, and control systems so agents do reliable work. (`harness-eng`)
- **Harness.** The execution system around the model: state, streaming, tools, sandbox and approval policy, carrying work across turns. In the improvement-loop sense, the full contract around the model: instructions, tools, routing, output requirements, validation. (`codex-platform`, `improvement-loop`)
- **Hill to climb.** A reviewed, measured finding packaged as an eval target for the coding agent. (`tax-agents`)
- **Human-supervised agent review.** Repeated investigator-agent audits of each flagged task, with a researcher making the final call. (`coding-evals-noise`)
- **In distribution.** Tool schemas and outputs formatted like data the model likely saw in training. (`perplexity-voice`)
- **Independent forward-testing.** Validating a skill by having a fresh sub-agent use it on a realistic request without hints. (`repo-skills`)
- **Independent web index.** A crawler-built list of public URLs, built without user data, used to decide what can be fetched automatically. (`link-safety`)
- **Input filter.** A function that rewrites what the receiving agent sees at a handoff. (`sdk-handoffs`)
- **Intervention / process / outcome-property evaluations.** The three archetypes: detect why behavior changed after a controlled change, detect which valid solution path was taken, predict a measurable output property such as reward hacking. (`cot-monitorability`)
- **Item / Turn / Thread.** An item is an atomic typed I/O unit with a lifecycle, a turn is one unit of agent work from user input, and a thread is a durable session of turns. (`app-server`)
- **Items.** Polymorphic output units that record each thing the model said or did, in order. (`responses-api`)
- **Iterative repair loop.** A closed loop where an agent produces output, validates it, and uses the feedback to improve the next pass. (`repair-loops`)
- **Know / do / show.** The three ways an app adds value: new context, real actions, better presentation. (`great-chatgpt-app`)
- **Language-first filtering.** Replacing filter controls with natural language mapped onto enumerated tool parameters. (`chatgpt-apps-lessons`)
- **Limitation.** An unknown (deployment, exposure) that does not change what the supplied source establishes. (`security-scanners`)
- **Living SOPs.** Skills as standard operating procedures updated as the org evolves and executed consistently by agents. (`skills-shell`)
- **Local context vs model context.** Local context is app state that tools and hooks use and the model never sees. Model context is whatever is in the conversation history when the model is called. (`sdk-context`)
- **Loop engineering.** Connecting feedback, testing, and implementation so agents improve from real behavior. (`improvement-loop`)
- **Mailbox.** The queue of messages and final-status notices a parent waits on. (`repo-tools`)
- **Manager pattern.** A central agent calls specialized agents as tools and keeps control of the user. (`practical-guide`)
- **Manifest.** A portable description of the agent's workspace: mounted files, output directories, data sources. (`agents-sdk-evolution`, `memory-compaction`)
- **Metaprompting.** Asking the model to propose edits to its own instructions after a weak turn. (`codex-prompting`)
- **Model visible context.** The message history sent to the model on each inference request, treated as a governed resource. (`repo-agents-md`)
- **Model-native harness.** A harness built around how OpenAI's models operate on files and tools, as opposed to a model-agnostic framework. (`agents-sdk-evolution`)
- **Monitorability.** How readable misbehavior is from the model's reasoning and tool traces. (`agent-monitoring`, `cot-monitorability`)
- **Monitorability tax.** The extra inference compute (small capability cost) paid to run a smaller model at higher reasoning effort for easier monitoring. (`cot-monitorability`)
- **Narrow test cases.** Tests that enforce specific implementation details and reject functionally correct submissions. (`swe-bench-verified`)
- **Negative control.** A prompt that should not trigger the skill, used to catch false-positive invocation. (`eval-skills`)
- **Optimistic execution.** The agent proceeds while guardrails run concurrently and raise if a constraint is breached. (`practical-guide`)
- **Oracle workflow.** Repo Prompt's pattern of a tool-less reasoning model that only analyzes a curated context package. (`responses-year`)
- **Overall correctness.** A final patch verdict meaning existing code and tests will not break and no blocking issues remain. (`repo-review`)
- **Persistent security weakening.** A security change that outlives the session and exposes an important service. (`repo-approvals`)
- **Phase 1 / Phase 2.** Per-rollout extraction, then cross-rollout consolidation. (`repo-memory`)
- **Policy as a package.** The guardrail config shipped as an installable, versioned package so every team gets the same policy. (`governance`)
- **Preamble messages.** One or two sentence notes sent before tool calls, grouping related actions and connecting to prior work (8 to 12 words for quick updates). (`repo-system-prompts`)
- **Preambles.** Short user updates sent alongside tool calls while the agent works. (`codex-prompting`)
- **Pre-flight.** The guardrail stage that runs before the input checks and the model call (PII, moderation). (`governance`)
- **Programmatic tool calling.** The model writes code that calls tools and filters or aggregates their outputs outside its context window. (`gpt56-guide`)
- **Progressive disclosure.** A skill root that routes to supporting material so the model reads only what the moment needs. (`astra-skills`, `repo-skills`)
- **Proof gap.** A missing fact that could change the judgment about a finding. While one remains, the finding can't be confirmed. (`security-scanners`)
- **Queue vs Steer.** A follow-up that waits for the current turn vs one injected into the turn in progress. (`codex-remote`)
- **Quick memory pass.** The bounded lookup (summary, then handbook grep, then 1-2 files) before main work. (`repo-memory`)
- **Rapid response loop.** Continuous cycle of automated attack discovery, adversarial training, and system fixes. (`atlas-injection`)
- **Remaining delta.** The structured list of what still fails after validation, fed into the next repair. (`repair-loops`)
- **Repo shard.** One task-sized slice of a migration with its own checkout and brief. (`code-migration`)
- **Report-first workflow.** A skill that inspects and prioritizes, then asks approval before editing. (`skills-oss`)
- **Repository rules.** Concise, directory-scoped review guidance in the agent instructions file that the reviewer can apply and cite. (`review-rules`)
- **Retained reasoning.** Keeping the model's private reasoning in context across tool calls and turns. (`arc-agi-3`)
- **Review packet.** The agent's deliverable for humans, including a video walkthrough of the feature working. (`symphony`)
- **Review rows.** Field-level records of expected vs predicted value and whether the difference looks actionable. (`tax-agents`)
- **Rolling discovery set.** Fresh production failures and near misses, promoted into the offline dataset when they expose a failure mode. (`realtime-evals`)
- **Rolling truncation.** Dropping the oldest messages once context exceeds a limit. (`arc-agi-3`)
- **Rollout / rollout summary.** One recorded agent session, and its distilled recap used as a memory source. (`repo-memory`)
- **Rule-supported finding.** A finding where a repository instruction materially contributes scope, an invariant, a remedy, or a convention beyond generic correctness. (`repo-review`)
- **Safe Url.** A mitigation that detects when information learned in the conversation would be sent to a third party and asks or blocks. (`injection-design`)
- **Sandbox as a tool.** The harness lives in trusted code and calls isolated compute only when it needs files, commands, or artifacts. (`claude-sdk-migration`)
- **Sensitive egress.** Moving non-public data anywhere an external party could access it. (`repo-approvals`)
- **Session / Task / Turn.** A session holds config and state, a task is the unit of work from one user input, and a turn is one model round trip plus its executions. (`repo-protocol`)
- **Side chat.** A lightweight conversation attached to the main chat for questions about the work, not the work itself. (`codex-remote`)
- **Sidecar task.** A bounded subtask that can run in parallel without blocking the agent's next local step. (`repo-tools`)
- **Source-sink analysis.** An attacker needs a source (a way to influence the system) and a sink (a capability dangerous in the wrong context). (`injection-design`)
- **Specify → Measure → Improve.** The three-step eval loop. (`business-evals`)
- **SQ / EQ.** Submission Queue (UI to engine ops) and Event Queue (engine to UI events). (`repo-protocol`)
- **Steering.** Adding instructions while the agent is already working. (`codex-maxxing`)
- **Sticky decision.** An always-approve or always-reject choice that applies to the same tool for the rest of the run. (`sdk-human-in-loop`)
- **Stop-and-fix rule.** Repair a failed validation before moving to the next milestone. (`long-horizon`)
- **Strict stop conditions.** The only states in which a long-running skill may end its loop. (`repo-skills`)
- **Symphony.** An orchestrator that turns a project board into a control plane where every open task gets an agent. (`symphony`)
- **Task-level preferences.** User style instructions that stay active until the task ends or the user changes them. (`repo-realtime`)
- **Thread automation.** A heartbeat-style recurring wake-up attached to an existing thread. (`codex-maxxing`)
- **Three body problem.** User, UI and model as three parties sharing state in an agent app. (`chatgpt-apps-lessons`)
- **Tier-aware family migration.** Mapping each existing model role to the matching tier of the new family instead of upgrading everything to the flagship. (`repo-prompting`)
- **Trace grading.** Scoring an agent's full run end to end to find where it went wrong. (`agentkit`)
- **Tripwire.** The signal a guardrail raises to halt the run immediately. (`sdk-guardrails`)
- **Turn.** The journey from user input to the agent's final message, possibly many inference and tool iterations. (`agent-loop`)
- **URL-based data exfiltration.** Tricking an agent into requesting a URL whose path or query carries private data. (`link-safety`)
- **Verification surface.** The test, benchmark, report, or artifact that proves the outcome. (`codex-goals`)
- **Verified wait.** A turn that polls a specific process or job handle confirmed live now, counted as neither progress nor stall. (`repo-goals`)
- **Visual thesis / interaction thesis.** One-sentence statements of mood and of 2-3 motion ideas the agent writes before building. (`frontends`)
- **Watch Mode.** The agent pauses on sensitive sites unless the user keeps the tab active and watches. (`prompt-injections`)
- **Wide test cases.** Tests that check functionality not specified in the problem description. (`swe-bench-verified`)
- **Workflows.** Recurring analyses packaged as reusable instruction sets. (`data-agent`)
